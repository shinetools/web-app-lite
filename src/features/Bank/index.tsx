import React, { FC } from 'react';
import { Title, Subtitle } from './styles';

const Bank: FC = () => (
  <div>
    <Title>Bank</Title>
    <Subtitle>Subtitle</Subtitle>
  </div>
);

export default Bank;
